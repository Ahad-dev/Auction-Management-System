// #ifndef FILEHANDLER_H
// #define FILEHANDLER_H

// #include <string>
// #include "LinkedList.h"
// #include "User.h"
// #include <fstream>
// // #include "Item.h"
// // #include "Bid.h"


// using namespace std;

// class FileHandler {
// public:
//     // Function to load users from file into a LinkedList
//     static void loadUsersFromFile(const string& filename, LinkedList<User*>& userList);

//     // Function to load items from file into a LinkedList
//     // static void loadItemsFromFile(const string& filename, LinkedList<Item*>& itemList);

//     // Function to load bids from file into a LinkedList
//     // static void loadBidsFromFile(const string& filename, LinkedList<Bid*>& bidList);

//     // Function to save users to file from a LinkedList
//     static void saveUsersToFile(const string& filename, LinkedList<User*>& userList);

//     // Function to save items to file from a LinkedList
//     // static void saveItemsToFile(const string& filename, LinkedList<Item*>& itemList);

//     // Function to save bids to file from a LinkedList
//     // static void saveBidsToFile(const string& filename, LinkedList<Bid*>& bidList);

//     //Authenticates the user
//     User* authenticateUser(const string& username, const string& password);
//     //Register the User
//     void resgisterUser(const string& username,const string& password,const string& role);

//     bool checkUsernameOrIdAleadyExist(const string& username, const string& id);


// };

// #endif
